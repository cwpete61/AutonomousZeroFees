{
    "version": "0.1.0",
    "name": "@agency/events",
    "main": "dist/index.js",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "clean": "rm -rf dist",
        "build": "tsc",
        "dev": "tsc --watch"
    },
    "types": "dist/index.d.ts",
    "private": true,
    "devDependencies": {
        "@nestjs/common": "^11.1.16",
        "@nestjs/config": "^4.0.3",
        "@types/ioredis": "^5.0.0",
        "ioredis": "^5.10.0"
    }
}
